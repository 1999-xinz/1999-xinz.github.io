<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>nodejs+sequelize数据库实操</title>
    <url>/p/f6eecfb1.html</url>
    <content><![CDATA[<p>技术栈：nodejs（type: module） + sequelize + mysql + dotenv-cli<br>
代码仓库地址：<a href="https://gitee.com/Lxinz/db-use.git">https://gitee.com/Lxinz/db-use.git</a></p>
<h2 id="Sequelize-基础准备">Sequelize 基础准备</h2>
<blockquote>
<p>默认你已经完成docker部署mysql，并创建了db_use的数据库，设置了数据库账号root/123456。</p>
</blockquote>
<p>1、如果没有安装sequelize、sequelize-cli、相关数据库（比如mysql2），需要先安装</p>
<p><code>npm install sequelize sequelize-cli mysql2</code></p>
<p>2、在对应目录下，初始化 Sequelize（生成配置、迁移、种子目录等）</p>
<p><code>npx sequelize-cli init</code></p>
<p><strong>目录结构：</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">project/</span><br><span class="line">├── config/</span><br><span class="line">│   ├── .<span class="property">env</span>.<span class="property">development</span>      <span class="comment">// 开发环境配置</span></span><br><span class="line">│   └── .<span class="property">env</span>.<span class="property">production</span>       <span class="comment">// 生产环境配置</span></span><br><span class="line">├── db/</span><br><span class="line">│   ├── config/</span><br><span class="line">│   │   └── config.<span class="property">js</span>         <span class="comment">// 数据库配置文件</span></span><br><span class="line">│   ├── models/</span><br><span class="line">│   │   └── index.<span class="property">js</span></span><br><span class="line">│   ├── migrations/</span><br><span class="line">│   │   └── [timestamp]-create-users-table.<span class="property">js</span></span><br><span class="line">│   └── seeders/</span><br><span class="line">├── .<span class="property">sequelizerc</span></span><br><span class="line">├── package.<span class="property">json</span></span><br><span class="line">└── ...</span><br></pre></td></tr></table></figure>
<p>3、数据库配置（用于迁移）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// config/config.js</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="attr">development</span>: &#123;</span><br><span class="line">    <span class="attr">username</span>: process.<span class="property">env</span>.<span class="property">DEV_DB_USERNAME</span>,</span><br><span class="line">    <span class="attr">password</span>: process.<span class="property">env</span>.<span class="property">DEV_DB_PASSWORD</span>,</span><br><span class="line">    <span class="attr">database</span>: process.<span class="property">env</span>.<span class="property">DEV_DB_DATABASE</span>,</span><br><span class="line">    <span class="attr">host</span>: process.<span class="property">env</span>.<span class="property">DEV_DB_HOST</span>,</span><br><span class="line">    <span class="attr">port</span>: process.<span class="property">env</span>.<span class="property">DEV_DB_PORT</span>,</span><br><span class="line">    <span class="attr">dialect</span>: process.<span class="property">env</span>.<span class="property">DEV_DB_DIALECT</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">production</span>: &#123;</span><br><span class="line">    <span class="attr">username</span>: process.<span class="property">env</span>.<span class="property">PROD_DB_USERNAME</span>,</span><br><span class="line">    <span class="attr">password</span>: process.<span class="property">env</span>.<span class="property">PROD_DB_PASSWORD</span>,</span><br><span class="line">    <span class="attr">database</span>: process.<span class="property">env</span>.<span class="property">PROD_DB_DATABASE</span>,</span><br><span class="line">    <span class="attr">host</span>: process.<span class="property">env</span>.<span class="property">PROD_DB_HOST</span>,</span><br><span class="line">    <span class="attr">port</span>: process.<span class="property">env</span>.<span class="property">PROD_DB_PORT</span>,</span><br><span class="line">    <span class="attr">dialect</span>: process.<span class="property">env</span>.<span class="property">PROD_DB_DIALECT</span>,</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>上面使用了dotenv来读取环境变量，需要安装npm install dotenv。</p>
</blockquote>
<p>这里使用了环境变量去配置数据库的信息，环境变量放置在根目录的config下（具体可查看目录结构）。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"># .<span class="property">env</span>.<span class="property">development</span></span><br><span class="line"><span class="variable constant_">NODE_ENV</span>=development</span><br><span class="line"><span class="variable constant_">DEV_DB_USERNAME</span>=root</span><br><span class="line"><span class="variable constant_">DEV_DB_PASSWORD</span>=<span class="number">123456</span></span><br><span class="line"><span class="variable constant_">DEV_DB_DATABASE</span>=db_use</span><br><span class="line"><span class="variable constant_">DEV_DB_HOST</span>=localhost</span><br><span class="line"><span class="variable constant_">DEV_DB_PORT</span>=<span class="number">3306</span></span><br><span class="line"><span class="variable constant_">DEV_DB_DIALECT</span>=mysql</span><br><span class="line"></span><br><span class="line"># .<span class="property">env</span>.<span class="property">production</span></span><br><span class="line"><span class="variable constant_">NODE_ENV</span>=production</span><br><span class="line"><span class="variable constant_">PROD_DB_USERNAME</span>=root</span><br><span class="line"><span class="variable constant_">PROD_DB_PASSWORD</span>=<span class="number">123456</span></span><br><span class="line"><span class="variable constant_">PROD_DB_DATABASE</span>=db_use</span><br><span class="line"><span class="variable constant_">PROD_DB_HOST</span>=localhost</span><br><span class="line"><span class="variable constant_">PROD_DB_PORT</span>=<span class="number">3306</span></span><br><span class="line"><span class="variable constant_">PROD_DB_DIALECT</span>=mysql</span><br></pre></td></tr></table></figure>
<p>上面环境变量通过dotenv-cli，在scripts脚本配置时，直接指向了对于目录的环境配置，因此可以在项目中直接使用process.env.NODE_ENV这样的方式调用环境变量。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// package.json的scripts区域中使用dotenv-cli参考</span></span><br><span class="line"> <span class="string">&quot;scripts&quot;</span>: &#123;</span><br><span class="line">   <span class="string">&quot;serve&quot;</span>: <span class="string">&quot;dotenv -e config/.env.development node index.js&quot;</span>,</span><br><span class="line">   <span class="string">&quot;serve:prod&quot;</span>: <span class="string">&quot;dotenv -e config/.env.production node index.js&quot;</span>,</span><br><span class="line">   <span class="string">&quot;migrate:up&quot;</span>: <span class="string">&quot;dotenv -e config/.env.development npx sequelize-cli db:migrate --env=development&quot;</span>,</span><br><span class="line">   <span class="string">&quot;migrate:undo&quot;</span>: <span class="string">&quot;dotenv -e config/.env.development npx sequelize-cli db:migrate:undo --env=development&quot;</span>,</span><br><span class="line">   <span class="string">&quot;migrate:up:prod&quot;</span>: <span class="string">&quot;dotenv -e config/.env.production npx sequelize-cli db:migrate --env=production&quot;</span>,</span><br><span class="line">   <span class="string">&quot;migrate:undo:prod&quot;</span>: <span class="string">&quot;dotenv -e config/.env.production npx sequelize-cli db:migrate:undo --env=production&quot;</span></span><br><span class="line"> &#125;,</span><br></pre></td></tr></table></figure>
<h2 id="Sequelize-表迁移步骤（生产推荐使用）">Sequelize 表迁移步骤（生产推荐使用）</h2>
<p>1、新建一个迁移文件</p>
<p><code>npx sequelize-cli migration:generate --name create-users-table</code></p>
<p>2、手动修改迁移文件内容（修改为ES语法、添加数据库字段）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="attr">up</span>: <span class="title function_">async</span> (queryInterface, <span class="title class_">Sequelize</span>) =&gt; &#123;</span><br><span class="line">    <span class="keyword">await</span> queryInterface.<span class="title function_">createTable</span>(<span class="string">&#x27;Users&#x27;</span>, &#123;</span><br><span class="line">      <span class="attr">id</span>: &#123;</span><br><span class="line">        <span class="attr">allowNull</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">autoIncrement</span>: <span class="literal">true</span>,</span><br><span class="line">        <span class="attr">primaryKey</span>: <span class="literal">true</span>,</span><br><span class="line">        <span class="attr">type</span>: <span class="title class_">Sequelize</span>.<span class="property">INTEGER</span>,</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">name</span>: &#123;</span><br><span class="line">        <span class="attr">type</span>: <span class="title class_">Sequelize</span>.<span class="property">STRING</span>,</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">email</span>: &#123;</span><br><span class="line">        <span class="attr">type</span>: <span class="title class_">Sequelize</span>.<span class="property">STRING</span>,</span><br><span class="line">        <span class="attr">unique</span>: <span class="literal">true</span>,</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">createdAt</span>: &#123;</span><br><span class="line">        <span class="attr">allowNull</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">type</span>: <span class="title class_">Sequelize</span>.<span class="property">DATE</span>,</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">updatedAt</span>: &#123;</span><br><span class="line">        <span class="attr">allowNull</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">type</span>: <span class="title class_">Sequelize</span>.<span class="property">DATE</span>,</span><br><span class="line">      &#125;,</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  <span class="attr">down</span>: <span class="title function_">async</span> (queryInterface, <span class="title class_">Sequelize</span>) =&gt; &#123;</span><br><span class="line">    <span class="keyword">await</span> queryInterface.<span class="title function_">dropTable</span>(<span class="string">&#x27;Users&#x27;</span>);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>await queryInterface.dropTable(‘Users’)作用？<br>
用于 <strong>回滚迁移的操作（undo migration）</strong>，当执行npx sequelize-cli db:migrate会运行 <code>up()</code> 方法，创建 <code>Users</code> 表；当执行npx sequelize-cli db:migrate:undo会运行 <code>down()</code> 方法，这时候 <code>dropTable('Users')</code> 就会生效，把 <code>Users</code> 表删掉。</p>
<p>需要注意的是，个迁移文件的话，<code>db:migrate:undo</code> 只会回滚最后一个迁移文件。如果你想一次性回滚所有迁移，则执行npx sequelize-cli db:migrate:undo:all即可。</p>
</blockquote>
<p>2、项目根目录配置.sequelizerc</p>
<p>这样可以在项目的根目录去执行迁移指令，或者封装到package.json的scripts中也可以正常使用了。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// .sequelizerc 文件</span></span><br><span class="line"><span class="comment">// 注意：sequelizerc的配置文件一定要使用commonjs规范，不能使用es6规范，否则使用sequelize-cli会无法识别配置信息</span></span><br><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&#x27;path&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;</span><br><span class="line">  <span class="string">&#x27;config&#x27;</span>: path.<span class="title function_">resolve</span>(<span class="string">&#x27;db&#x27;</span>, <span class="string">&#x27;config&#x27;</span>, <span class="string">&#x27;config.js&#x27;</span>),</span><br><span class="line">  <span class="string">&#x27;models-path&#x27;</span>: path.<span class="title function_">resolve</span>(<span class="string">&#x27;db&#x27;</span>, <span class="string">&#x27;models&#x27;</span>),</span><br><span class="line">  <span class="string">&#x27;seeders-path&#x27;</span>: path.<span class="title function_">resolve</span>(<span class="string">&#x27;db&#x27;</span>, <span class="string">&#x27;seeders&#x27;</span>),</span><br><span class="line">  <span class="string">&#x27;migrations-path&#x27;</span>: path.<span class="title function_">resolve</span>(<span class="string">&#x27;db&#x27;</span>, <span class="string">&#x27;migrations&#x27;</span>),</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>3、执行迁移</p>
<p>在项目根目录下执行：</p>
<p><code>npx sequelize-cli db:migrate --env=development</code></p>
<p>其中的 <code>development</code> 参数指的是你在 <code>sequelize</code> 初始化的 <code>config/config.js</code> 文件中配置的某一个环境；如果你不加 <code>--env</code> 参数，Sequelize CLI 默认使用 <code>development</code> 环境。</p>
<h2 id="Sequelize-数据迁移-种子操作（Seed）">Sequelize 数据迁移-种子操作（Seed）</h2>
<p>1、创建种子文件</p>
<p><code>npx sequelize-cli seed:generate --name update-user</code></p>
<p>2、编辑种子文件</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">up</span> (queryInterface, <span class="title class_">Sequelize</span>) &#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Add seed commands here.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * Example:</span></span><br><span class="line"><span class="comment">     * await queryInterface.bulkInsert(&#x27;People&#x27;, [&#123;</span></span><br><span class="line"><span class="comment">     *   name: &#x27;John Doe&#x27;,</span></span><br><span class="line"><span class="comment">     *   isBetaMember: false</span></span><br><span class="line"><span class="comment">     * &#125;], &#123;&#125;);</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">await</span> queryInterface.<span class="title function_">bulkInsert</span>(<span class="string">&#x27;Users&#x27;</span>, [&#123;</span><br><span class="line">      <span class="attr">name</span>: <span class="string">&#x27;John Doe&#x27;</span>,</span><br><span class="line">      <span class="attr">email</span>: <span class="string">&#x27;12345678910@qq.com&#x27;</span>,</span><br><span class="line">      <span class="attr">createdAt</span>: <span class="keyword">new</span> <span class="title class_">Date</span>(),</span><br><span class="line">      <span class="attr">updatedAt</span>: <span class="keyword">new</span> <span class="title class_">Date</span>()</span><br><span class="line">    &#125;], &#123;&#125;);</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">down</span> (queryInterface, <span class="title class_">Sequelize</span>) &#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Add commands to revert seed here.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * Example:</span></span><br><span class="line"><span class="comment">     * await queryInterface.bulkDelete(&#x27;People&#x27;, null, &#123;&#125;);</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">await</span> queryInterface.<span class="title function_">bulkDelete</span>(<span class="string">&#x27;Users&#x27;</span>, <span class="literal">null</span>, &#123;&#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>如果只更新表中指定的某条数据的部分字段，可以在up函数中的queryInterface.bulkInsert方法的第二个参数传入一个对象，第三个参数传入查询参数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">await</span> queryInterface.<span class="title function_">bulkUpdate</span>(</span><br><span class="line">  <span class="string">&#x27;Users&#x27;</span>,</span><br><span class="line">  &#123; <span class="attr">name</span>: <span class="string">&#x27;李四&#x27;</span> &#125;, <span class="comment">// 更新内容</span></span><br><span class="line">  &#123; <span class="attr">id</span>: <span class="number">1</span> &#125; <span class="comment">// where 条件</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>
<p>3、执行种子插入</p>
<p><code>npx sequelize-cli db:seed:all --env=development</code></p>
<h2 id="增删改查实操">增删改查实操</h2>
<p>1、在db/models下创建对应数据库的表模型，比如User.js，内容可以参考迁移文件中对应的表进行编写。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="title function_">default</span> (sequelize, <span class="title class_">DataTypes</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="title class_">Users</span> = sequelize.<span class="title function_">define</span>(<span class="string">&#x27;Users&#x27;</span>, &#123;</span><br><span class="line">    <span class="attr">id</span>: &#123;</span><br><span class="line">      <span class="attr">type</span>: <span class="title class_">DataTypes</span>.<span class="property">INTEGER</span>,</span><br><span class="line">      <span class="attr">autoIncrement</span>: <span class="literal">true</span>,</span><br><span class="line">      <span class="attr">primaryKey</span>: <span class="literal">true</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">name</span>: &#123;</span><br><span class="line">      <span class="attr">type</span>: <span class="title class_">DataTypes</span>.<span class="property">STRING</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">email</span>: &#123;</span><br><span class="line">      <span class="attr">type</span>: <span class="title class_">DataTypes</span>.<span class="property">STRING</span>,</span><br><span class="line">      <span class="attr">unique</span>: <span class="literal">true</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;, &#123;</span><br><span class="line">    <span class="comment">// 启动时间，设置为ture会自动生成创建和更新时间，默认字段名称为createAt、updateAt。</span></span><br><span class="line">    <span class="attr">timestamps</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="comment">// 对应的表名将与model名相同</span></span><br><span class="line">    <span class="attr">freezeTableName</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="comment">// 表备注</span></span><br><span class="line">    <span class="attr">comment</span>: <span class="string">&#x27;用户表&#x27;</span>,</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Users</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>需要注意的是，如果迁移表中创建了createdAt、updatedAt等时间自动，可以在表模型文件中通过配置属性timestamps去实现自动创建和更新，也就不需要在表模型的字段定义中再次定义了。</p>
<p>如果是先写了表模型文件，想编写对应的迁移文件，就需要注意，如果有timestamps字段定义的，需要在迁移文件中自行创建。</p>
</blockquote>
<p>之后，会在db/models/index.js中自动引入，并导出sequelize实例和表模型的一个对象db，通过db就可以进行数据库对应表的增删改查了。</p>
<p>2、比如以上面创建的Users表为例，在项目的根目录的入口index.js中实现增删改查。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// index.js</span></span><br><span class="line"><span class="keyword">import</span> db <span class="keyword">from</span> <span class="string">&#x27;./db/models/index.js&#x27;</span>;</span><br><span class="line"><span class="keyword">const</span> &#123; <span class="title class_">Users</span> &#125; = db;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 增加</span></span><br><span class="line"><span class="keyword">const</span> newUser = <span class="keyword">await</span> <span class="title class_">Users</span>.<span class="title function_">create</span>(&#123; <span class="attr">name</span>: <span class="string">&#x27;Tom&#x27;</span>, <span class="attr">email</span>: <span class="string">&#x27;tom@example.com&#x27;</span> &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询</span></span><br><span class="line"><span class="keyword">const</span> <span class="title class_">User</span> = <span class="keyword">await</span> <span class="title class_">Users</span>.<span class="title function_">findOne</span>(&#123; <span class="attr">where</span>: &#123; <span class="attr">email</span>: <span class="string">&#x27;tom@example.com&#x27;</span> &#125; &#125;);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">User</span>.<span class="title function_">toJSON</span>());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 更新</span></span><br><span class="line"><span class="keyword">await</span> <span class="title class_">Users</span>.<span class="title function_">update</span>(&#123; <span class="attr">name</span>: <span class="string">&#x27;Tommy&#x27;</span> &#125;, &#123; <span class="attr">where</span>: &#123; <span class="attr">email</span>: <span class="string">&#x27;tom@example.com&#x27;</span> &#125; &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除</span></span><br><span class="line"><span class="keyword">await</span> <span class="title class_">Users</span>.<span class="title function_">destroy</span>(&#123; <span class="attr">where</span>: &#123; <span class="attr">email</span>: <span class="string">&#x27;tom@example.com&#x27;</span> &#125; &#125;);</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>nodejs</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>Deepseek私有化部署</title>
    <url>/p/a3a30358.html</url>
    <content><![CDATA[<blockquote>
<p>本篇文章记录Deepseek私有化部署以及本地知识库的搭建流程。</p>
</blockquote>
<h2 id="1-方案">1-方案</h2>
<p>方案采用ollama+anythingllm+docker私有化部署deepseek-r1:8b。</p>
<blockquote>
<p>ollama：大模型的管理和运行工具。</p>
<p>anythingllm：实现文档聊天，本地知识库（支持多个文本检索）的可视化应用。</p>
<p>docker部署anythingllm，实现Web网页访问，非anythingllm官网的桌面访问，是因为Web网页访问的文档聊天更加快速精准以及支持更多个性化设置，如果后续进行接口二次开发，就更要选择docker部署方案。</p>
</blockquote>
<h2 id="2-部署">2-部署</h2>
<p><strong>1、前往ollama官网，下载对应系统（window/macos）的应用。</strong></p>
<p>ollama官网：<a href="https://ollama.com/">https://ollama.com/</a></p>
<p><strong>2、ollama安装完成后，打开系统终端，执行下面指令，安装并运行大模型。</strong></p>
<p><code>ollama run deepseek-r1:8b</code></p>
<blockquote>
<p>建议40系列以上显卡或macos m1芯片性能及以上的运行8b，其余运行7b或以下。</p>
<p>有哪些模型选择可以参考下面ollama官网模型版本选择：<br>
<a href="https://ollama.com/library/deepseek-r1">https://ollama.com/library/deepseek-r1</a></p>
</blockquote>
<p><strong>3、运行成功后，可以在终端进行测试。</strong></p>
<p><img src="/p/a3a30358/image-20250220095323890.png" alt="image-20250220095323890"></p>
<p>执行ctrl+D可以退出终端聊天。</p>
<p><strong>4、前往Docker Desktop官网，向下滑动，找到下载即可</strong></p>
<p>Docker Desktop官网：<a href="https://www.docker.com/products/docker-desktop/">https://www.docker.com/products/docker-desktop/</a></p>
<img src="/p/a3a30358/image-20250220100007725.png" alt="image-20250220100007725" style="zoom:50%;">
<blockquote>
<p>下载安装Docker Desktop过程中可能存在需要科学上网的情况，否则无法完成登录，搜索远程镜像等操作。</p>
<p>可以参考网上的安装教程：<br>
<a href="https://blog.csdn.net/Natsuago/article/details/145588600">https://blog.csdn.net/Natsuago/article/details/145588600</a></p>
<p>建议是涉及到Docker Desktop登录或搜索远程镜像等联网操作时，全程进行科学上网，需要科学上网的途径可以使用下面的网址：（完成注册+登录+查看官网教程安装工具，选择并开启代理）</p>
<p><a href="https://www.sibker.com/register?code=VIev6i3i">https://www.sibker.com/register?code=VIev6i3i</a></p>
</blockquote>
<p><strong>5、Docker Desktop安装完成后，按照如下步骤部署anythingllm</strong></p>
<p>打开系统终端，执行下面指令，查看docker是否安装成功。</p>
<p><code>docker -v</code></p>
<p><img src="/p/a3a30358/image-20250220101122242.png" alt="image-20250220101122242"></p>
<p>在系统终端中，执行下面指令，拉取远程镜像mintplexlabs/anythingllm</p>
<p><code>docker pull mintplexlabs/anythingllm</code></p>
<p>之后，在终端复制下方对应系统的指令，去运行镜像，建立本地知识库映射。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Linux/MacOs</span><br><span class="line">export STORAGE_LOCATION=$HOME/anythingllm &amp;&amp; \</span><br><span class="line">mkdir -p $STORAGE_LOCATION &amp;&amp; \</span><br><span class="line">touch &quot;$STORAGE_LOCATION/.env&quot; &amp;&amp; \</span><br><span class="line">docker run -d -p 3001:3001 \</span><br><span class="line">--cap-add SYS_ADMIN \</span><br><span class="line">-v $&#123;STORAGE_LOCATION&#125;:/app/server/storage \</span><br><span class="line">-v $&#123;STORAGE_LOCATION&#125;/.env:/app/server/.env \</span><br><span class="line">-e STORAGE_DIR=&quot;/app/server/storage&quot; \</span><br><span class="line">mintplexlabs/anythingllm</span><br><span class="line"></span><br><span class="line"># Windows</span><br><span class="line">$env:STORAGE_LOCATION=&quot;$HOME\Documents\anythingllm&quot;; `</span><br><span class="line">If(!(Test-Path $env:STORAGE_LOCATION)) &#123;New-Item $env:STORAGE_LOCATION -ItemType Directory&#125;; `</span><br><span class="line">If(!(Test-Path &quot;$env:STORAGE_LOCATION\.env&quot;)) &#123;New-Item &quot;$env:STORAGE_LOCATION\.env&quot; -ItemType File&#125;; `</span><br><span class="line">docker run -d -p 3001:3001 `</span><br><span class="line">--cap-add SYS_ADMIN `</span><br><span class="line">-v &quot;$env:STORAGE_LOCATION`:/app/server/storage&quot; `</span><br><span class="line">-v &quot;$env:STORAGE_LOCATION\.env:/app/server/.env&quot; `</span><br><span class="line">-e STORAGE_DIR=&quot;/app/server/storage&quot; `</span><br><span class="line">mintplexlabs/anythingllm;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>直接运行上面指令后，前往游览器访问<a href="http://localhost:3001">http://localhost:3001</a>，现在就可以使用anythingllm了！</p>
<p>你会看到下面的界面：</p>
<p><img src="/p/a3a30358/image-20250220101959304.png" alt="image-20250220101959304.png"></p>
<p>点击开始，进行<strong>AnythingLLM</strong>配置，选择Ollama，填入 Ollama 的 <code>http://localhost:11434</code> 端口，然后选择下载的deepseek-r1模型，之后一直选择下一步，进入到聊天首页。</p>
<p>模型配置可以参考：</p>
<p><img src="/p/a3a30358/image-20250220102817145.png" alt="image-20250220102817145.png"></p>
<p>嵌入引擎配置参考：</p>
<p><img src="/p/a3a30358/image-20250220102852762.png" alt="image-20250220102852762"></p>
<p>上面嵌入引擎使用了nomic-embed-text模型，需要自行在终端执行下载，用于知识库上传的文件向量化。</p>
<p>ollama模型地址：<a href="https://ollama.com/library/nomic-embed-text">https://ollama.com/library/nomic-embed-text</a></p>
<p><img src="/p/a3a30358/image-20250220103248172.png" alt="image-20250220103248172"></p>
<p><strong>6、接下来就可以进行知识库上传，进行聊天/查询</strong></p>
<img src="/p/a3a30358/image-20250220103718365.png" alt="image-20250220103718365" style="zoom:50%;">
<p>知识库上传后，才可以进行，查询操作，我们需要上传知识库。<br>
<code>&lt;img src=&quot;./Deepseek私有化部署/image-20250220104205120.png&quot; alt=&quot;image-20250220104205120&quot; style=&quot;zoom:50%;&quot; /&gt;</code></p>
<p>选择公共区域的知识移到自己的工作区，并进行知识向量化。</p>
<img src="/p/a3a30358/image-20250220104336494.png" alt="image-20250220104336494" style="zoom:50%;">
<img src="/p/a3a30358/image-20250220104531299.png" alt="image-20250220104531299" style="zoom:50%;">
<p>之后，如果在聊天需要引用知识库，则在向量化后，点击图钉。</p>
<img src="/p/a3a30358/image-20250220104758795.png" alt="image-20250220104758795" style="zoom:50%;">
<p>接下来就可以开始聊天了，比如，我添加了一篇百度文章的知识后，就可以询问它相关知识内容。</p>
<img src="/p/a3a30358/image-20250220104934329.png" alt="image-20250220104934329" style="zoom: 50%;">
<img src="/p/a3a30358/image-20250220105118672.png" alt="image-20250220105118672" style="zoom:50%;">
<p>7、如果需要个性化定制或api开发，可以在设置中找到。</p>
<img src="/p/a3a30358/image-20250220105230788.png" alt="image-20250220105230788">
<img src="/p/a3a30358/image-20250220105259265.png" alt="image-20250220105259265" style="zoom:50%;">
<h2 id="3-其他知识扩充">3-其他知识扩充</h2>
<p><strong>1、大模型问答流程图：</strong><br>
<img src="/p/a3a30358/image-20250220162833675.png" alt="image-20250220162833675"></p>
<p>大模型问答基本流程说明：</p>
<ul>
<li>用户查询内容：用户首先输入查询内容，系统将根据用户的请求开始处理。</li>
<li>查询内容输入到嵌入式模型：查询内容被送入一个嵌入式模型，该模型将内容进行向量化（或编码）处理，转换成机器可以理解的表示方式。</li>
<li>向量化：在嵌入式模型中，查询内容被转化为向量化表示，为下一步的检索提供依据。</li>
<li>检索数据库：将向量化后的查询内容与数据库中的大量文档进行匹配，系统从数据库中检索相关的文档或信息。</li>
<li>增强提示词：为进一步优化查询效果，系统可以根据检索到的内容生成增强提示词，来进一步引导查询。</li>
<li>相关文档返回查询内容：根据生成的增强提示词，系统返回与查询内容最相关的文档或信息，作为最终的响应内容。</li>
<li>生成式语言模型：除了检索外，系统还会通过生成式语言模型来生成与查询相关的内容，补充或优化检索到的信息。</li>
<li>最终响应：生成式语言模型的输出内容将作为最终的响应返回给用户。</li>
</ul>
<p><strong>2、大模型 API 接口的速度衡量标准参考：</strong></p>
<ul>
<li>RPM (Requests Per Minute)：每分钟可以处理的请求数量。</li>
<li>RPD (Requests Per Day)‌：每天可以处理的请求数量。</li>
<li>IPM (Images Per Minute)‌：每分钟可以处理的图像数量。</li>
<li>TPM (Tokens Per Minute)：每分钟可以处理的令牌数量。</li>
<li>TPD (Tokens Per Day)：每天可以处理的令牌数量。</li>
<li>响应时间：从请求应用端发送到大模型收到响应的时间。</li>
</ul>
]]></content>
      <categories>
        <category>llm大模型</category>
      </categories>
      <tags>
        <tag>llm大模型</tag>
      </tags>
  </entry>
  <entry>
    <title>IP可用端口扫描器工具（bun + typescript）</title>
    <url>/p/40cc747c.html</url>
    <content><![CDATA[<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">学习方式：源码学习。</span><br><span class="line"></span><br><span class="line">通过项目和源码可以学习到如下内容：</span><br><span class="line"></span><br><span class="line">1、bun搭建项目，打包项目</span><br><span class="line"></span><br><span class="line">2、net、dns等node内置模块的使用</span><br><span class="line"></span><br><span class="line">3、yargs、assert、progress、cli-color等三方包的使用</span><br><span class="line"></span><br><span class="line">ps：内置模块查询nodejs官方API介绍；三方模块查询NPM官网对应包信息。</span><br><span class="line"></span><br><span class="line">nodejs官方API地址: [https://nodejs.org/docs/latest/api/](https://nodejs.org/docs/latest/api/)</span><br><span class="line"></span><br><span class="line">NPM官网地址：[https://www.npmjs.com/](https://www.npmjs.com/)</span><br></pre></td></tr></table></figure>
<h2 id="前言">前言</h2>
<p>1、项目介绍：</p>
<p>项目通过bun搭建nodejs项目，实现扫描检测指定IP下哪些端口可用。项目使用net、dns实现ip端口的连接检测和域名解析端口的方法；使用了yargs实现用户终端输入参数；使用了assert、cli-color实现了终端错误信息提示和终端好看的输出内容颜色以及使用了progress美化终端输出扫描进度。</p>
<p>2、核心技术栈：</p>
<p>bun、net、yargs、typescript。</p>
<p>3、项目源码地址(github)：</p>
<p><a href="https://github.com/1999-xinz/xinz-port-scanner.git">https://github.com/1999-xinz/xinz-port-scanner.git</a></p>
<p>ps: 有帮助的话，可以给个stars，谢谢</p>
<h2 id="1-项目运行">1-项目运行</h2>
<p><strong>1、执行下面指令，运行项目</strong></p>
<p><code>bun start scan &lt;ip&gt; &lt;portrange&gt;</code></p>
<p><img src="/p/40cc747c/1730800235520.png" alt="1730800235520"></p>
<p>categories:</p>
<ul>
<li>后端</li>
</ul>
<blockquote>
<p>1、start是package.json中script的配置。</p>
<p>2、如果没有bun的需要进行全局安装。</p>
<p>macOS安装方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">curl -fsSL https://bun.sh/install | bash</span><br></pre></td></tr></table></figure>
<p>建议查看bun安装介绍，地址：<a href="https://bun.sh/docs/installation">https://bun.sh/docs/installation</a></p>
</blockquote>
<h2 id="2-核心源码说明">2-核心源码说明</h2>
<p>源码在根目录src下index.ts中，在源码中优先找到最下面的main入口主函数，从主函数的代码逐步理解功能实现。</p>
<p>main主函数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * 入口主函数</span><br><span class="line"> */</span><br><span class="line">async function main() &#123;</span><br><span class="line">  let host = ip;</span><br><span class="line">  </span><br><span class="line">  // 判断是否是域名，如果是则需要进行域名解析</span><br><span class="line">  if (!isIpCheck(host)) &#123;</span><br><span class="line">    host = await resolveDomain(host);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  // 断言校验</span><br><span class="line">  assert(isIpCheck(host), `IP地址不合理: $&#123;ip&#125;`)</span><br><span class="line">  assert(isPortRangeCheck(portrange), `端口范围不合理: $&#123;portrange&#125;`)</span><br><span class="line"></span><br><span class="line">  let ports = portrange.split(&#x27;-&#x27;).map(Number); // 转换为数字数组</span><br><span class="line">  await scanPorts(host, ports[0], ports[1]);</span><br><span class="line">&#125;</span><br><span class="line">// 执行，执行完成后终止进程任务</span><br><span class="line">main()</span><br><span class="line">  .then(() =&gt; process.exit(0)) // 成功退出</span><br><span class="line">  .catch((err) =&gt; &#123; // 如果失败，输出错误并退出码为 1</span><br><span class="line">    console.error(err);</span><br><span class="line">    process.exit(1); // 失败退出</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>
<p>上面主函数执行时，给主函数使用了async，之后确保main函数执行完成后，终止当前任务进程执行process.exit(0)，如果出错，就捕获且打印错误，并退出进程process.exit(1)。</p>
<p>需要注意的是，在主函数执行之前，执行了yargs方法，用于获取终端用户输入的ip和端口范围的值，之后存储到全局变量ip和portrange，之后在main函数中一开始进行获取和后续处理。</p>
<p>yargs方法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** </span></span><br><span class="line"><span class="comment"> * yargs 命令行参数配置初始化</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> <span class="attr">ip</span>: string;</span><br><span class="line"><span class="keyword">let</span> <span class="attr">portrange</span>: string;</span><br><span class="line"><span class="title function_">yargs</span>(</span><br><span class="line">  <span class="title function_">hideBin</span>(process.<span class="property">argv</span>))</span><br><span class="line">    .<span class="title function_">command</span>(<span class="string">&#x27;scan &lt;ip&gt; &lt;portrange&gt;&#x27;</span>, <span class="string">&#x27;scan the given IP and port range&#x27;</span>, </span><br><span class="line">    <span class="function">(<span class="params">yargs</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> yargs</span><br><span class="line">        <span class="comment">// 默认扫描本机</span></span><br><span class="line">        .<span class="title function_">positional</span>(<span class="string">&#x27;ip&#x27;</span>, &#123;</span><br><span class="line">          <span class="attr">describe</span>: <span class="string">&#x27;扫描ip地址(支持输入域名)&#x27;</span>,</span><br><span class="line">          <span class="attr">type</span>: <span class="string">&#x27;string&#x27;</span>,</span><br><span class="line">          <span class="attr">default</span>: <span class="string">&#x27;127.0.0.1&#x27;</span></span><br><span class="line">        &#125;)</span><br><span class="line">        <span class="comment">// 默认扫描所有端口</span></span><br><span class="line">        .<span class="title function_">positional</span>(<span class="string">&#x27;portrange&#x27;</span>, &#123;</span><br><span class="line">          <span class="attr">describe</span>: <span class="string">&#x27;扫描端口范围(端口范围：1-65535 或者指定某个端口：2000-2000)&#x27;</span>,</span><br><span class="line">          <span class="attr">type</span>: <span class="string">&#x27;string&#x27;</span>,</span><br><span class="line">          <span class="attr">default</span>: <span class="string">&#x27;1-65535&#x27;</span></span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;, </span><br><span class="line">    <span class="function">(<span class="params">argv</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="comment">// 调用main函数, 传递参数</span></span><br><span class="line">      ip = argv.<span class="property">ip</span>;</span><br><span class="line">      portrange = argv.<span class="property">portrange</span>;</span><br><span class="line">    &#125;)</span><br><span class="line">    .<span class="title function_">demandCommand</span>(<span class="number">1</span>)</span><br><span class="line">    .<span class="title function_">help</span>()  <span class="comment">// 添加帮助信息</span></span><br><span class="line">    .<span class="title function_">alias</span>(<span class="string">&#x27;h&#x27;</span>, <span class="string">&#x27;help&#x27;</span>)  <span class="comment">// 添加帮助信息</span></span><br><span class="line">    .<span class="title function_">parse</span>()</span><br></pre></td></tr></table></figure>
<p>其它方法模块，可以根据执行函数模块的注释进行理解即可。</p>
<h2 id="3-项目打包">3-项目打包</h2>
<p><strong>1、执行下面指令，进行项目打包</strong></p>
<p><code>bun run build</code></p>
<p>之后，会在项目根目录构建build目录，目录下就是打包后的入口文件。</p>
<p>打包完成后，还需要将package.json和node_modules复制过去，这样才是一个完整的生产环境包。</p>
<p><strong>2、运行build下的入口文件方式</strong></p>
<p>可以使用node或者bun进行运行，一般服务器上安装node即可，那么默认使用node（版本需要大于16.x.x）吧，操作如下：</p>
<p><code>node index.js scan &lt;ip&gt; &lt;portrange&gt;</code></p>
<p><img src="/p/40cc747c/1730800304444.png" alt="1730800304444"></p>
]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>nodejs/Bun</tag>
      </tags>
  </entry>
  <entry>
    <title>通用文件和大文件分片上传（bun + express）</title>
    <url>/p/f9b92fdd.html</url>
    <content><![CDATA[<p>通用文件上传和大文件分片上传（断点/断网续传）的后台服务（bun + express）。</p>
<h2 id="前言">前言</h2>
<p>本项目实现了通用文件上传和大文件分片上传功能，支持断点续传、断网续传等特性。通过简单的接口，用户可以上传小文件，也可以对大文件进行分片上传，确保在断网或中途停止的情况下，上传任务可以从中断处继续进行。该项目适用于需要文件上传功能的各种应用场景。</p>
<p>项目源码地址(github)：<br>
<a href="https://github.com/1999-xinz/xinz-bun-upload.git">https://github.com/1999-xinz/xinz-bun-upload.git</a></p>
<p>ps: 有帮助的话，可以给个stars，谢谢</p>
<h2 id="项目功能">项目功能</h2>
<ul>
<li>支持普通文件上传。</li>
<li>支持大文件分片上传，支持断点/断网续传。</li>
<li>支持swagger文档查看相关接口（共4个）。</li>
<li>提供前端页面进行接口测试。</li>
<li>本地简单模拟数据库的实现，方便学习重心放在上传的服务上。</li>
</ul>
<h2 id="如何启动">如何启动</h2>
<p><strong>1-安装依赖（如果没有bun的请先安装bun）：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bun install</span><br></pre></td></tr></table></figure>
<p><strong>2-运行项目：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bun run src/app.ts 或 bun run start</span><br></pre></td></tr></table></figure>
<h2 id="项目目录说明">项目目录说明</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">├── pages/                   <span class="comment"># 前端上传测试页面</span></span><br><span class="line">├── server/                  <span class="comment"># 后端服务项目目录</span></span><br><span class="line">│   ├── routes/              <span class="comment"># 路由配置文件，定义上传接口</span></span><br><span class="line">│   ├── swagger/             <span class="comment"># swagger基础配置文件目录</span></span><br><span class="line">│   ├── utils/              <span class="comment"># 工具函数（主要编写了模拟数据库crud操作）</span></span><br><span class="line">│   └── app.ts               <span class="comment"># 应用主入口文件，配置和启动 Express、swagger 服务，托管前端页面服务等</span></span><br><span class="line">├── tmp/</span><br><span class="line">│   ├── db/                  <span class="comment"># 模拟数据库，把预校验分片的分片索要进行文本长期存储，用于断点续传</span></span><br><span class="line">│   └── uploads              <span class="comment"># 存放上传的文件（可配置到其他位置）</span></span><br><span class="line">├── package.json             <span class="comment"># 项目依赖和脚本配置</span></span><br><span class="line">├── bun.lockb                <span class="comment"># Bun 的锁文件，确保项目依赖一致</span></span><br><span class="line">└── README.md                <span class="comment"># 项目说明文档</span></span><br></pre></td></tr></table></figure>
<p>建议先从app.ts为入口，去查看相关接口文件、配置文件的代码。</p>
<p>在启动项目后，可以通过访问下面地址来进行接口测试，静态页面用express进行了托管（server/app.ts中）。</p>
<ul>
<li>通用文件上传：<a href="http://127.0.0.1:3000/www/commonPage.html">http://127.0.0.1:3000/www/commonPage.html</a></li>
<li>分片文件上传：<a href="http://127.0.0.1:3000/www/chunkPage.html">http://127.0.0.1:3000/www/chunkPage.html</a></li>
</ul>
<p>上传的文件会存放到tmp/uploads/目录下面，包括分片文件也是在这下面，分片文件会在合并后删除。</p>
<h2 id="项目测试效果和接口文档">项目测试效果和接口文档</h2>
<p><strong>1-上传中的效果：</strong></p>
<p><img src="/p/f9b92fdd/1733296476283.png" alt="1733296476283"></p>
<p><strong>2-上传成功的效果：</strong></p>
<p>1、分片上传：</p>
<p><img src="/p/f9b92fdd/1733300754904.png" alt="1733300754904"></p>
<p>2、通用上传：</p>
<p><img src="/p/f9b92fdd/1733300736974.png" alt="1733300736974"></p>
<p><strong>3-接口文档：</strong></p>
<p><img src="/p/f9b92fdd/1733296568448.png" alt="1733296568448"></p>
]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>nodejs/Bun</tag>
      </tags>
  </entry>
</search>
